/**
 * manual = Manual
 * automatic = Automático
 * cvt = CVT
 * semi_auto = Semi-Automático
 * dual_clutch = Dupla Embreagem (DCT)
 */
export type TransmissionType = "manual" | "automatic" | "cvt" | "semi_auto" | "dual_clutch"
